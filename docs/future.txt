read MediaPlayer docs

FEATURES
google assistant integration
  watch youtube googleio videos
  codelabs to refresh memory


ondone (tab ime keyboard) tab from minutes field to seconds field

recent section, show list past timers, unique, reverse order,
last 3...


dagger/hilt - circular dependencies use Lazy on one side
callback closures can be in a separate class .e.g. class ExitOverlay, with invoke fn


add exit pendingintent to stopwatch notification  

test with bigger font scaling